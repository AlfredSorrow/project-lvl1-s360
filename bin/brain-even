#!/usr/bin/env php
<?php
require __DIR__ . '/../vendor/autoload.php';
use function \BrainGames\Cli\run;
use function \BrainGames\even\even;

$gameDescription = 'Answer "yes" if number even otherwise answer "no".';
$name = run($gameDescription);
even($name);